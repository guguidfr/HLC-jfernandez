# Pedir al usuario 2 valores: el primero es la 'tabla' en la que hay que buscar; el segundo es el 'multiplicando'.
# Hay que buscar el archivo de la tabla correspondiente, si existe, se busca la linea que coincida con la multiplicación que queremos hacer, y la mostramos por pantalla; si no existe, se muestra un mensaje de error.